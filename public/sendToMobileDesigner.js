/**
 * IDE --> MOBILE DESIGNER 메세지 송신시 데이터 처리 로직
 */
import globalService from "@/service/globalservice";

(function () {
    /**
     * IDE -> 모바일 디자이너
     * @param {string} [args] IDE에서 디자이너로 데이터
     */
    function SendToMobileDesigner(args) {
        globalService.test.test();
    }
})
