/*
 * 컨트롤 생성후 반환
 */


/*
let createControl = (type, parent, option, obj) => {
    let control;
    switch (type.toUpperCase()) {
        case "CONTAINERPANEL" :
    }
    //window.createControl = control;
    return control;
};

export {createControl};
*/
